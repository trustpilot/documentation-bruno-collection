meta {
  name: Get list of invitation templates (private)
  type: http
  seq: 1
}

get {
  url: {{stagingurlInvitation}}/private/business-units/{{bundingoBusinessUnitId}}/templates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
