meta {
  name: Get conversation
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/private/conversations/:conversationId
  body: none
  auth: bearer
}

params:path {
  conversationId: {{conversationId}}
}

auth:bearer {
  token: {{token}}
}
